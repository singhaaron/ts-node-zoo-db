const VIEW_ANIMAL_EXHIBITS: string = `select * from Exhibit_Animals;`;
const VIEW_CUSTOMERS: string = `select * from Customers;`;

const _ = {
  VIEW_ANIMAL_EXHIBITS,
  VIEW_CUSTOMERS,
};

export default _;
